#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int a, b, c, d;
    scanf("%d%d%d%d", &a, &b, &c, &d);
    printf("Khac biet = %d", a * b - c * d);
    return 0;
}